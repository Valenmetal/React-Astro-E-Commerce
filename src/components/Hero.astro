<div class="hero">
    <h1 class="title">Zingueria <br /> Marquez</h1>
    <img src="/src/img/DSC_0363.jpg" alt="Hero Image" />
</div>

<style lang="scss">
    .hero {
        img {
            filter: blur(2px);
            object-fit: cover;
            height: 100vh;
            width: 100%;
        }
        .title {
            color: var(--white);
            text-shadow: black -10px 10px 10px;
            z-index: 10;
            font-size: var(--step-6);
            font-weight: 800;
            position: absolute;
            letter-spacing: 5px;
            top: 10%;
            left: 5%;
        }
    }

    @media screen and (max-width: 800px) {
    }
</style>
